import type { ComponentProps } from 'react';
import { clsx } from 'clsx';

import styles from './{{kebabCase name}}.module.scss';

export type {{properCase name}}Props = ComponentProps<'div'> & {}

export const {{properCase name}} = (props: {{properCase name}}Props) => {
    const { className, children, ...restProps } = props;

    return (
        <div className={clsx(styles.root, className)} {...restProps}>
            { children }
        </div>
    );
};

{{properCase name}}.displayName = '{{properCase name}}';
